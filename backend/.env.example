MONGO_URI=mongodb+srv://username:password@cluster.example.mongodb.net/notes-app
JWT_SECRET=replace-with-a-long-random-secret
PORT=5000
# Comma-separated frontend origins. Leave empty only when frontend and API share one origin.
CLIENT_ORIGIN=http://localhost:5173
